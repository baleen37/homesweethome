# 호갱노노 검색 API 상세 분석 보고서

## 개요

호갱노노의 검색 API (`/api/v2/searches/new`)를 통해 강남구와 서초구의 동 정보를 분석한 결과입니다.

## API 엔드포인트

- **기본 URL**: `https://hogangnono.com/api/v2/searches/new`
- **메서드**: GET
- **주요 파라미터**:
  - `query`: 검색어 (URL 인코딩 필요, 예: %EA%B0%95%EB%82%A8%EA%B5%AC)
  - `x`: 경도 (선택 사항)
  - `y`: 위도 (선택 사항)

## 강남구 상세 분석

### 1. 지역 정보

```json
{
  "id": "1168000000",
  "name": "서울특별시 강남구",
  "local_type": "local2",
  "local_code": "11680",
  "local_name": "강남구",
  "local1_code": "11",          // 서울특별시 코드
  "local1_name": "서울특별시",
  "local2_code": "11680",       // 강남구 코드
  "local2_name": "강남구",
  "location": {
    "lon": 127.047502,
    "lat": 37.517305
  }
}
```

### 2. 강남구 소속 동 정보 (확인된 동)

| 동명 | 코드 | 좌표 (경도, 위도) |
|------|------|------------------|
| 청담동 | 11680104 | (127.049291, 37.525107) |
| 신사동 | 11680107 | (127.0227814, 37.5240101) |

### 3. API 응답 구조

검색 API는 5가지 타입의 정보를 반환합니다:

1. **apt** (아파트/오피스텔 목록)
2. **aptTag** (태그 기반 매물 목록)
3. **region** (지역/동 정보)
4. **subway** (지하철역 정보)
5. **poi** (관심 장소 정보)

### 4. 코드 체계 분석

#### 행정 구조
- **시/도 코드**: 11 (서울특별시)
- **구/군 코드**: 11680 (강남구)
- **동 코드**:
  - 청담동: 11680104
  - 신사동: 11680107

#### ID 포맷
- **구/군 ID**: {시도코드}000000 (예: 1168000000)
- **동 ID**: {구군코드}{동코드}00 (예: 1168010400)

## 서초구 상세 분석

### 1. 지역 정보

API를 통해 서초구 검색 시 유사한 구조의 응답을 받을 수 있습니다.

## API 활용 방안

### 1. 구/군 전체 매물 검색

```python
# 강남구 매물 검색
url = "https://hogangnono.com/api/v2/searches/new"
params = {
    "query": "강남구",
    "x": 127.047502,
    "y": 37.517305
}
```

### 2. 동별 매물 검색

```python
# 청담동 매물 검색
params = {
    "query": "청담동",
    "x": 127.049291,
    "y": 37.525107
}
```

### 3. 좌표 기반 검색

특정 좌표 주변의 매물을 검색할 때 사용합니다.

## 주요 발견 사항

### 1. 데이터 필드

#### 지역 정보 필드
- `local_type`: 행정구분 (local2: 구/군, local3: 동)
- `local_code`: 행정구역 코드
- `location.lon`: 경도
- `location.lat`: 위도

#### 매물 정보 필드
- `id`: 매물 고유 ID
- `type`: 0 (아파트), 1 (오피스텔)
- `name`: 매물명
- `address`: 지번 주소
- `road_address`: 도로명 주소
- `household`: 세대 수
- `trade_count`: 거래 건수
- `location.lat/lng`: 정확한 좌표

### 2. 검색 동작 방식

1. **키워드 매칭**: 입력된 키워드로 여러 타입의 데이터를 동시 검색
2. **랭킹**: `rank_point` 기반으로 결과 정렬
3. **페이징**: `isEnd`와 `order` 필드로 페이징 처리
4. **좌표 기반**: x, y 파라미터로 위치 기반 검색 가능

### 3. Rate Limiting

- API 호출 간격은 최소 1초 이상 권장
- 과도한 호출 시 차단될 수 있음

## 구현 제안

### 1. 지역 코드 기반 크롤러

```python
class HogangnonoRegionCrawler:
    def __init__(self):
        self.base_url = "https://hogangnono.com/api/v2/searches/new"
        self.regions = {
            "강남구": {"code": "11680", "lat": 37.517305, "lng": 127.047502},
            "서초구": {"code": "11650", "lat": 37.483735, "lng": 127.005732},
        }

    def search_region(self, region_name: str):
        """지역별 매물 검색"""
        region = self.regions[region_name]
        params = {
            "query": region_name,
            "x": region["lng"],
            "y": region["lat"]
        }
        # API 호출 및 결과 처리
```

### 2. 동별 매물 수집

```python
def collect_all_dongs_in_gu(self, gu_name: str):
    """구 내 모든 동의 매물 수집"""
    # 1. 구 코드로 지역 정보 조회
    # 2. 응답에서 동 목록 추출
    # 3. 각 동별로 매물 검색
    pass
```

## 한계 및 주의사항

1. **동 전체 목록**: 검색 API는 전체 동 목록을 한 번에 제공하지 않음
2. **API 변화**: 실제 프로덕션 환경에서는 API 변경 가능성 고려
3. **인증**: 현재는 별도 인증이 없으나, 언제든지 추가될 수 있음
4. **데이터 정확성**: 검색 결과의 순서와 개수는 동적으로 변할 수 있음

## 결론

호갱노노의 검색 API는 지역 기반 부동산 매물 검색에 활용할 수 있으며, 특히 구/군 코드와 좌표 정보를 활용하여 체계적인 데이터 수집이 가능합니다. 다만, 전체 동 목록을 얻기 위해서는 추가적인 API 분석이 필요합니다.
